{% extends("base.html.twig") %}
{% block body %}

    <div class="container">

        {{ form_start(form) }}
        {{ form_row(form.country) }}
        {{ form_row(form.year) }}
        {{ form_row(form.submit) }}
        {{ form_end(form) }}

    </div>

{% endblock %}